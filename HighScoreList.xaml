<Window x:Class="Binder.HighScoreList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Binder"
        mc:Ignorable="d"
        Title="HighScores" Height="450" Width="800"
        MinHeight="450"
        MaxHeight="450"
        MinWidth="800"
        MaxWidth="800">
    <Window.Background>
        <ImageBrush ImageSource="Screen.PNG"/>
    </Window.Background>
    <Canvas>
        <TextBlock Text="High Scores" FontFamily="Haettenschweiler" FontSize="60" Foreground="#e4c027" Width="220" Canvas.Left="295" Canvas.Top="15"/>
        <!--<ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" Height="260" Width="400" Canvas.Left="200" Canvas.Top="100">
            <TextBox x:Name="txtScores" Text="" FontSize="20"/>
        </ScrollViewer>-->
    </Canvas>
</Window>
